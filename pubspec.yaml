name: track_slider
description: Track Slider is a powerful and customizable track slider built with love using Flutter and Dart.
version: 0.0.6
repository: https://github.com/uroojk844/track_slider

environment:
  sdk: ">=3.1.5 <4.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

platforms:
  android:
  ios:
  linux:
  macos:
  web:
  windows:
